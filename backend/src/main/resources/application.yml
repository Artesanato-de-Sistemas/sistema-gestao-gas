spring:
    datasource:
      url: jdbc:postgresql://db.mhxycuagyraepaxmpekg.supabase.co:5432/postgres
      username: postgres
      password: fagocitose!
    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQL.Dialect