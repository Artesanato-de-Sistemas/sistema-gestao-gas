services:
  postgres:
    container_name: sgg-postgres
    image: postgres:18
    environment:
        - POSTGRES_USER=postgres
        - POSTGRES_PASSWORD=corinthians10
    ports:
      - '5432:5432'
    volumes:
      - postgres:/var/lib/postgresql/
volumes:
  postgres: